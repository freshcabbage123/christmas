# .hacktron/config.yaml
# Repo: freshcabbage123/christmas
# Org: begeha7535
#
# Generated to preserve your current covered-branch selection
# ("main") as PR scanning moves from the
# dashboard UI into this file. Commit this to the repository's
# default branch at .hacktron/config.yaml.

include:
  branches:
    - "main"
